package main

import "fmt"
import "net"
import "os"
import "io/ioutil"

func main() {
	service:=":7777"
	
}
func checkError(err error) {
	if err != nil {
		fmt.Fprintf(os.Stderr, "Fatal error:%s", err.Error())
		os.Exit(1)
	}
}
